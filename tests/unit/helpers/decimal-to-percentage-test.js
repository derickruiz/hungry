import { decimalToPercentage } from 'hungry/helpers/decimal-to-percentage';
import { module, test } from 'qunit';

module('Unit | Helper | decimal to percentage');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decimalToPercentage([42]);
  assert.ok(result);
});
